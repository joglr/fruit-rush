// import { State } from "./../modules/State"

// const defaultState = {
//   hello: "world"
// }

// let state

beforeEach(() => {
  // state = new State(defaultState)
})

describe("State", () => {
  it.todo("update the state when passed a new state object")
  it.todo("update the state when passed a state modifier")
})
